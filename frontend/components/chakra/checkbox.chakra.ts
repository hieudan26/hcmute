import { ComponentStyleConfig } from '@chakra-ui/react';

export const CheckBox: ComponentStyleConfig = {
  baseStyle: {
    colorScheme: 'pink',
  },
  defaultProps: {
    colorScheme: 'pink',
  },
};
